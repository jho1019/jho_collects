# eBay transaction reports land here

Drop the Seller Hub → Payments → Reports → **Transaction report** CSV in this
folder (or attach it in a Claude Code session and let the `ebay-import` skill
save it here). Everything here except this file is gitignored — the reports
carry buyer PII and must never be committed.
